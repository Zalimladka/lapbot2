services:
  - type: web
    name: fb-group-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py & python change_nick.py"
    envVars:
      - key: PORT
        value: 10000
